{
  "name": "studyspark",
  "private": true,
  "version": "1.0.0",
  "description": "StudySpark static site — package.json exists solely so Netlify installs dependencies for netlify/functions/claude-proxy.js",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
